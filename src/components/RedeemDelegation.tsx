"use client"

import { TweetData } from "@/types/TweetData"

export default function RedeemDelegation(props: TweetData) {
    return <>TODO: use a mav1 session account to redeem delegation by sending a tx to the xLock Plugin</>
}